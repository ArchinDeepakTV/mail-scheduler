{
  "name": "mail-scheduler",
  "version": "1.0.0",
  "description": "Schedule emails to be sent at a future time. Deploys to Vercel, stores jobs in MongoDB.",
  "main": "index.js",
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "nodemailer": "^6.9.15"
  }
}
