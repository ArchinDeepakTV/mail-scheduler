{
  "name": "mail-scheduler",
  "version": "1.0.0",
  "description": "Schedule emails to be sent at a future time. Deploys to Vercel, stores jobs in MongoDB.",
  "engines": {
    "node": "=24.19.0"
  },
  "scripts": {
    "dev": "node server.local.js",
    "test": "node test.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "nodemailer": "^6.9.15",
    "dotenv": "^16.4.5"
  }
}
